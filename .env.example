# Used for local (non-Docker) backend runs
DATABASE_URL=postgresql://todos:todos@localhost:5432/todos

API_TARGET=http://localhost:8000

VITE_PROXY_TARGET=http://localhost:8000

# Optional: point the React app at a remote API during local Vite dev
# Leave empty to use the Vite proxy (/api -> http://localhost:8000)
# VITE_API_URL=http://localhost:8000
